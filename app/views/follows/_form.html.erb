
<% text = user.followers.include?(current_user) ? "followed" : "unfollowed"%>
<button class="follow-toggle" 
        data-user-id="<%= user.id%>" 
        data-initial-follow-state="<%= text %>">
</button> 

